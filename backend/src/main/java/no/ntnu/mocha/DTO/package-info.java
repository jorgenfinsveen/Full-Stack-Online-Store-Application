/**
 * Contains classes which represents Data Transfer Objects (DTO). The classes are being used
 * to represent database entity types in a simpler way, making them convenient to use for e.g. 
 * passing data to the server through the REST API endpoints. The classes are also convenient
 * to share between different modules, reducing coupling between classes which does not need to 
 * access the actual entity class itself.
 * 
 * @version 29.05.2023
 * @author IDATA2306, Group 10
 */
package no.ntnu.mocha.DTO;
