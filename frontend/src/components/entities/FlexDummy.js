import "./FlexDummy.css"

/**
 * Component representing a dummy element.
 * Used for layout with flexbox elements.
 * 
 * @returns {JSX.Element} dummy div
 */
export function FlexDummy() {
    return (
        <div className="dummy"></div>
    );
}